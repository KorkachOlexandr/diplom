# Account 4 — Assignment 3: deliberately empty

Do NOT upload any file. With this account, open the assignment and click
**Turn in** (or "Mark as done") with no attachment.

This exercises the pipeline's "no attachments" extraction-error path —
the submission shows up in the dashboard with an extraction note rather
than crashing the scan.
